import { createStyleSheet } from '@/utils/theme/createStyleSheet';

export const styles = createStyleSheet({
  siderWrapper: {
    background: '#fff',
  },
  mainWrapper: {
    flex: 1,
    height: 0,
    overflow: 'auto',
  },
});
